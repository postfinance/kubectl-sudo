#!/bin/sh

read -p "WARNING: Currently in context [4m$(kubectl config current-context)[0m. Continue? (y/N) " yn
case $yn in
    [Yy]* ) ;;
    * ) exit 1;;
esac

kubectl --as=$USER --as-group=system:masters "$@"
