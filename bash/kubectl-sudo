#!/bin/sh

BC=$'\e[4m'
EC=$'\e[0m'

read -p "WARNING: Currently in context ${BC}$(kubectl config current-context)${EC}. Continue? (y/N) " yn
case $yn in
    [Yy]* ) ;;
    * ) exit 1;;
esac

kubectl --as=$USER --as-group=system:masters "$@"
